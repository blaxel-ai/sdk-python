name: Python SDK release

on:
  push:
    tags:
      - "*"

jobs:
  pypi-publish:
    name: Upload blaxel package to PyPI
    runs-on: ubuntu-latest
    environment: release
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - name: "Install uv"
        uses: astral-sh/setup-uv@v4
      - name: Set version
        env:
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
        run: |
          VERSION="${{ github.ref_name }}"
          VERSION="${VERSION#v}"
          echo "Setting version to $VERSION"
          sed -i -E "s/^version = .*/version = \"${VERSION}\"/" pyproject.toml
          sed -i -E "s|^sentry_dsn = .*|sentry_dsn = \"${SENTRY_DSN}\"|" pyproject.toml
      - name: Test package installation
        run: |
          uv venv
          source .venv/bin/activate
          uv pip install -e .
          python -c "import blaxel.core; print('✅ blaxel package imports successfully')"
      - name: Build and publish blaxel package
        run: |
          uv build
          uv publish -v --trusted-publishing automatic dist/*
